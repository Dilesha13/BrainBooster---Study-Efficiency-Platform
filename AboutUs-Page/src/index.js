import React from 'react';
import ReactDOM from 'react-dom';
import Team from './AboutUs';

ReactDOM.render(
  <React.StrictMode>
    <Team />
  </React.StrictMode>,
  document.getElementById('root')
);